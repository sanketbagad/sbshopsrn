import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    title: {
        fontSize: 20,
    },
    price: {
        fontSize: 16,
        fontWeight: 'bold',
    },
    description: {},
    
})

export default styles;